package lab5.parsers;

public class JsonFormatter {
    public JsonFormatter(){
        super();
    }
    /*public String reformat(String jsonStr) {
        boolean inQuotes = false;
        String new_s = "";
        String tabs = "";
        for (int i = 0; i < jsonStr.length(); i++) {
            char sim = jsonStr.charAt(i);
            if (!inQuotes) {
                if (jsonStr.charAt(i) == '{')

            }
            if (jsonStr.charAt(i) == '"') {
                inQuotes = !inQuotes;
            }

            new_s += char
        }
        return new_s;
    }*/
}
